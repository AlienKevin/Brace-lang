func input($str, $var){
	Input $str,A
	$var=A
	//local variables
	$doubledVar=$var*2
	$var=$doubledVar
}
$var=32
$var2=65
Str1 = "What's your name?"
input(Str1, $name)
Disp "should display your name:"
Disp $name
$var1=32
$var2=40
$var3=$var1+$var2
Disp "$var3 should be: 72"
Disp $var3
input ("A=",$A)
Disp "should disp: A=0"
/*
multi-line comments
*/
if $A==0{
	Disp "A",$A
	if $B==0{
		Disp "B",$B
	} else{
		if $C==0{
			Disp "C",$C
		} else{
			if $D==0{
				Disp "D",$D
			}
		}
	}
}